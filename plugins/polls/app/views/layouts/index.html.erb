<!DOCTYPE html>
<html lang="<%= current_language %>">
<head>
<meta charset="utf-8" />
<meta http-equiv="X-UA-Compatible" content="IE=edge"/> 
<title><%= html_title %></title>
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
<meta name="description" content="<%= Redmine::Info.app_name %>" />
<meta name="keywords" content="issue,bug,tracker" />
<meta name="renderer" content="webkit|ie-comp|ie-stand">
<%= csrf_meta_tag %>
<%= favicon %>
<%= stylesheet_link_tag 'H-ui','H-ui.admin', 'iconfont.min','skin','style',:media => 'all' %>
<script type="text/javascript" src="/javascripts/jquery.min.js"></script>
<link rel="stylesheet" type="text/css" href="/stylesheets/context_menu.css">
<script type="text/javascript" src="/javascripts/context_menu.js"></script>
<link rel="stylesheet" type="text/css" href="/stylesheets/application.css">
<link rel="stylesheet" type="text/css" href="/stylesheets/jquery/jquery-ui-1.11.0.css">
<%= javascript_heads_plugin %>
<%= heads_for_theme %>
<!-- page specific tags -->
<%= yield :header_tags -%>
</head>
<body>
<!--_header 作为公共模版分离出去-->
<header class="navbar-wrapper">
    <div class="navbar navbar-fixed-top">
        <div class="container-fluid cl">
            <a class="logo navbar-logo f-l mr-10 hidden-xs" href="javascript:viod(0)" style="color: white;">BMDG资产管理系统</a>
            <nav id="Hui-userbar" class="nav navbar-nav navbar-userbar hidden-xs" style="margin-right: 50px;">
                <ul class="cl">
                    <li>您好!<%= User.current.try(:name)%></li>
                    <li class="dropDown dropDown_hover">
                        <a href="#" class="dropDown_A">操作</a>
                        <ul class="dropDown-menu menu radius box-shadow">
                            <li><a href="/" >主页</a></li>
                            <li>
                                 <a href="/logout?plugin=true" method="post" rel="nofollow">退出</a> 
                            </li>
                        </ul>
                    </li>
                </ul>
            </nav>
        </div>
    </div>
</header>
<!--/_header 作为公共模版分离出去-->

<!--_menu 作为公共模版分离出去-->
<aside class="Hui-aside">
    
    <div class="menu_dropdown bk_2">
        <% Project.first_show.each do |project|%>
            <dl id="menu-<%= project.id%>">
                <dt class="<%= project.children.map(&:id).include?(params[:project_id].to_i) ? 'selected' : '' %>"><%= project.name %></dt>
                <dd style="<%= project.children.map(&:id).include?(params[:project_id].to_i) ? 'display: block' : '' %>">
                    <ul>
                        <% if project.children.present? %>
                            <% project.children.each_with_index do |pr, index| %>
                                <li class="<%= pr.id == params[:project_id].to_i ? 'current' : '' %>">
                                   <%= link_to pr.name, point_check_list_polls_path(project_id:pr.id)%>
                                </li>
                            <% end %>
                        <% end %>
                    </ul>
                </dd>
            </dl>
        <% end %>
    </div>
</aside>
<div class="dislpayArrow hidden-xs">
    <a class="pngfix" href="javascript:void(0);" onClick="displaynavbar(this)"></a>
</div>
<!--/_menu 作为公共模版分离出去-->
<style type="text/css">
    .hui_article{
        position: none;
    }
</style>
<section class="Hui-article-box" style="overflow-y: auto;">
    <%= render partial: "/shared/flash_msg"%>
     <%= yield %>
 </section>
</body>

</html>
